import React from "react";
import { Typography, Box } from "@mui/material";
export default function SignupPage() {
  return (
    <Box p={4}>
      <Typography variant="h4">Sign Up</Typography>
      <Typography>Signup form coming soon...</Typography>
    </Box>
  );
}